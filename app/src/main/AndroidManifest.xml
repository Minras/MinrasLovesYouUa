<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.minras.lovesyou">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/unicorn_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/unicorn_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CopyrightActivity"
            android:label="@string/title_activity_copyright"
            android:parentActivityName=".MainActivity">
            <intent-filter>
                <!-- Parent activity meta-data to support 4.0 and lower -->
                <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="com.minras.lovesyou.MainActivity" />
            </intent-filter>
        </activity>
    </application>

</manifest>